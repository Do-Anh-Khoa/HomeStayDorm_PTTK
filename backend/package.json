{
  "name": "homestay-dorm-be",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "prisma generate && node --watch src/server.js",
    "start": "prisma generate && node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "verify:logout": "node scripts/verifyLogout.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcryptjs": "latest",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^9.0.0"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
